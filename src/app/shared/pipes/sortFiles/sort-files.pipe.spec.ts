import { SortFilesPipe } from './sort-files.pipe';

describe('SortFilesPipe', () => {
    it('create an instance', () => {
        const pipe = new SortFilesPipe();
        expect(pipe).toBeTruthy();
    });
});
